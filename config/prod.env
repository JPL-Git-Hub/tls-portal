# Production Environment Configuration
NODE_ENV=production
PORT=3001
FRONTEND_PORT=3000

# Firebase - Production settings
USE_EMULATOR=false
FIREBASE_PROJECT_ID=the-law-shop-457607
GOOGLE_APPLICATION_CREDENTIALS=./config/.secrets/firebase-service-account.json

# URLs - Update these with your production domains
API_BASE_URL=https://api.thelawshop.com
FRONTEND_URL=https://thelawshop.com
ALLOWED_ORIGINS=https://thelawshop.com,https://*.thelawshop.com

# JWT configuration (use strong secret in production)
JWT_SECRET=production-secret-stored-in-google-secrets-manager
JWT_EXPIRES_IN=7d

# Domain configuration
DOMAIN=thelawshop.com
SUBDOMAIN_PATTERN={subdomain}.thelawshop.com

# Features
ENABLE_SIGNUP=true
ENABLE_EMULATORS=false
LOG_LEVEL=info

# Stripe Configuration (production keys)
# These should be in config/.secrets/stripe-keys.env
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_PUBLISHABLE_KEY=pk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...